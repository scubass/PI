#include <stdio.h>

int
main(void)
{
	return 0;
}

int
suma(int x, int y) {
	int respuesta;
	respuesta = x + y;
}

// error el programa no hace nada, si quisiera devolver la sua con un return x + y estaría, pero no sería útil la función además no devuelve nada y especifica int
